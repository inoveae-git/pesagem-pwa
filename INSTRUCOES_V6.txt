ALTERAÇÕES NO INDEX.HTML
1) Display manual:
<div id="display" class="display-numero" onclick="digitarManual()">---</div>

2) Nome do PDF:
const dataNome = dataStr.replace(/\//g,'-');
const horaNome = horaStr.replace(':','-');
const nomeArquivo = `${dest.replace(/\s+/g,'_')}_${dataNome}_${horaNome}.pdf`;
doc.save(nomeArquivo);

3) Total peso 3 colunas:
let pesoTotalGeral = 0;
No loop: pesoTotalGeral += somaKg;
Final:
doc.text(`PESO TOTAL GERAL: ${pesoTotalGeral.toFixed(2)} kg`, 10, 136);
